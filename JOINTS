join is used to combine rows from two or more tables and based on related column between them

innre joints -erturn the value that have common values in both table

SELECT*
FROM BACHA
INNER JOIN COURSE
ON COURSE.ID=BACHA.ID

LEFT TABLE ALL RECORD FROM LEFT TABLE AND MATCHED RECORD FROM RIGHT TABLE

SELECT*
FROM BACHA-->LEFT TABLE
LEFT JOIN COURSE--->RIGHT TABLE
ON COURSE.ID=BACHA.ID

RIGHT TABLE

SELECT*
FROM BACHA
RIGHT JOIN COURSE
ON COURSE.ID=BACHA.ID

TO TAKE THE FULL TABLE AND COMMAN VALUES ALSO

SELECT*
FROM BACHA
RIGHT JOIN COURSE
ON COURSE.ID=BACHA.ID
UNION
SELECT*
FROM BACHA
LEFT JOIN COURSE
ON COURSE.ID=BACHA.ID;

ONLY LEFT

SELECT*
FROM BACHA AS B
RIGHT JOIN COURSE AS C
ON C.ID=B.ID
WHERE B.ID IS NULL;


SUBQUERRIES ARE USED TO COMBINE TWO DIFFERENT STATEMENTS
select avg(marks)
 from bacha
 
 SELECT stu_name
 FROM bacha
 WHERE MARKS>AVG;

QUERIES USED

 SELECT stu_name
 FROM bacha
 WHERE MARKS>(select avg(marks) from bacha);

SELECT STU_NAME,ID
 FROM BACHA
 WHERE MARKS IN(SELECT STU_NAME FROM BACHA WHERE MARKS%2=0);
